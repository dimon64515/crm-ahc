# AGENTS.md — Справка для AI-ассистентов

> Этот файл предназначен для AI-агентов, работающих с проектом. Вся документация, комментарии в коде, сообщения пользовательского интерфейса и пользовательские запросы ведутся на **русском языке** — при внесении изменений придерживайтесь того же стиля.
>
> Файл составлен на основе актуального состояния кода. Часть документации в `docs/` описывает желаемое/устаревшее поведение; при расхождении доверяйте коду и этому файлу.

---

## Обзор проекта

**CRM АХЧ** — кастомная CRM-система для заместителя директора по административно-хозяйственной части (АХЧ). Продакшен-домен: `https://report.fanat-mv.ru`.

Приложение предназначено для:

- учёта работ подрядчиков на объектах (корпусах);
- управления справочниками материалов и видов работ/услуг;
- формирования отчётности и выгрузки в Excel/Word;
- хранения фотофиксации и документов по работам;
- создания резервных копий.

---

## Роутер для AI-агентов

- Работаешь с **backend** (API, модели, бизнес-логика) — читай `AGENTS_BACKEND.md`.
- Работаешь с **frontend** (компоненты, маршруты, стили) — читай `AGENTS_FRONTEND.md`.
- Настраиваешь **запуск, тесты или деплой** — читай `AGENTS_DEPLOY.md`.

---

## Ролевая модель

| Роль | Ключ | Права |
|------|------|-------|
| **Подрядчик** | `contractor` | Работает с заявками, берёт их в работу, создаёт отчёты только по назначенным заявкам. Не видит дашборд, цены и настройки. |
| **Директор** | `director` | Дашборд, отчёты, управление заявками, архив фото. Не редактирует записи и цены. |
| **Администратор** | `admin` | Всё, что у директора, плюс пользователи, корпуса, справочники, редактирование отчётов, цены, бэкапы, журнал аудита. |
| **Комендант** | `comendant` | Создание заявок и просмотр своих заявок. |

Проверка прав реализована в `backend/app/core/dependencies.py`:

- `require_admin` — только `admin`
- `require_director` — `director` и `admin`
- `require_contractor` — `contractor`, `director` и `admin`
- `get_current_user` — любой авторизованный пользователь

---

## Ключевая структура проекта

```
crm/
├── backend/          # FastAPI-приложение
│   ├── app/
│   │   ├── main.py
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── core/
│   │   ├── routers/
│   │   └── services/
│   ├── alembic/
│   ├── tests/
│   └── requirements.txt
├── frontend/         # React + Vite
│   ├── src/
│   │   ├── App.jsx
│   │   ├── api.js
│   │   ├── contexts/
│   │   ├── components/
│   │   └── pages/
│   └── package.json
├── docs/             # Документация
├── uploads/          # Файловое хранилище
├── docker-compose.yml
└── README.md
```

---

## Общие соглашения

- Язык документации, комментариев и пользовательских строк — **русский**.
- Backend: Python, PEP 8, FastAPI, SQLAlchemy, Pydantic.
- Frontend: React, JSX, inline-стили + глобальные CSS-классы.
- API-вызовы frontend централизованы в `frontend/src/api.js`.
- Миграции БД — Alembic, скрипты в `backend/alembic/versions/`.

---

## Полезные ссылки

- `AGENTS_BACKEND.md` — детали backend.
- `AGENTS_FRONTEND.md` — детали frontend.
- `AGENTS_DEPLOY.md` — запуск, тесты, деплой.
- `docs/architecture.md` — архитектура и потоки данных (частично устарело).
- `docs/api_specification.md` — спецификация REST API.
- `docs/database_schema.md` — схема БД.
- `docs/deployment.md` — полная инструкция по деплою.
- `docs/reports.md` — отчёты и дашборд.
